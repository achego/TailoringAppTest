import 'package:flutter/material.dart';

class Senator extends StatelessWidget {
  const Senator({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Senator');
  }
}
