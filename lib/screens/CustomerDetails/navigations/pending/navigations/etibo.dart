import 'package:flutter/material.dart';

class Etibo extends StatelessWidget {
  const Etibo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Etibo');
  }
}
